# Assumptions

- The project’s UI is implemented with Textual (Python) and has a runnable `App` entry point.
- `textual serve` is a viable approach for our supported Textual version.
- The app does not rely on terminal-only capabilities that cannot be mapped to the web frontend.
