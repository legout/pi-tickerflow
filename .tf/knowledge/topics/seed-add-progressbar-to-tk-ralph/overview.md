# seed-add-progressbar-to-tk-ralph

Add an optional progress bar to `tk ralph` runs so humans can see high-level progress without verbose per-step logging.
Also support a mode that suppresses Pi/agent output (or routes it to a log file) so the terminal stays clean.

## Keywords

- ralph
- progressbar
- cli
- logging
- quiet-mode
- non-interactive
