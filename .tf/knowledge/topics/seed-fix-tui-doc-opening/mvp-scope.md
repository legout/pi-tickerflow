# MVP Scope

## In Scope
1. Move or delegate action methods to `TicketflowApp`
2. Add `self.app.suspend()` wrapper around `os.system()` call
3. Basic testing with `less` pager

## Out of Scope
- Tab-aware key handling (can be added later)
- Additional keyboard shortcuts
- Changes to document format
- Non-TTY support improvements
