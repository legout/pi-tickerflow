# Constraints

- Must not break existing keyboard navigation in the TUI
- Must handle cases where no topic is selected gracefully
- Must work with common pagers (`less`, `more`, `cat`) and editors (`vim`, `nano`)
- Should not require changes to the document file format or directory structure
