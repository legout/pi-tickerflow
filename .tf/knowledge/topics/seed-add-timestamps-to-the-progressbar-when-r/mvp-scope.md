# MVP Scope

## In Scope

- Add timestamp prefix to `--progress` output for the serial Ralph loop.
- Add/adjust unit tests for progress output formatting.
- Update docs/help text if progress output is documented.

## Out of Scope (for MVP)

- Parallel-mode progress UI.
- A full formatting config matrix (timezone selection, locale, etc.).
- Persisting timing metrics beyond printing timestamps.
