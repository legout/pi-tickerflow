# Success Metrics

- With `tf ralph start --progress`, progress lines visibly include timestamps.
- Logs captured from stderr/stdout remain readable and parsable.
- No change in output when `--progress` is not used.
- Unit tests cover the timestamp formatting behavior.
