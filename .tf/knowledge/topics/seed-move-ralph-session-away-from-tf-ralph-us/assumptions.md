# Assumptions

- Pi has a stable, documented session directory concept.
- `tf ralph` can be updated to choose a new default without breaking the workflow.
- Users may want per-project configuration overrides.
