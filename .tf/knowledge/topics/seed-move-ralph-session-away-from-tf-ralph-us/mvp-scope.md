# MVP Scope

## In Scope

- Identify Pi standard session directory and implement it as the default for Ralph sessions.
- Add config/env override to restore old behavior if desired.
- Document behavior in `tf ralph --help`.

## Out of Scope (MVP)

- Automatic migration of historical session archives beyond a simple copy/notice.
- Any change to JSONL session format.
