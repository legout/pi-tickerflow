# Success Metrics

- Progress display total matches the number of tickets in the backlog at start (no more 50 default).
- Running `tf ralph start --progress` shows intuitive progress (done/total).
- No behavioral change when `--progress` is not used.
